server:
  port: 8080

spring:
  rabbitmq:
    host: ${SPRING_RABBITMQ_HOST:mq.gravifox.com}
    port: ${SPRING_RABBITMQ_PORT:5000}
    username: ${SPRING_RABBITMQ_USERNAME:gravifox}
    password: ${SPRING_RABBITMQ_PASSWORD:!Tmdals017217}
    virtual-host: ${SPRING_RABBITMQ_VIRTUAL_HOST:/}
    ssl:
      enabled: true
      algorithm: TLSv1.3

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/member?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: root

front:
  redirect:
    url: http://localhost:3000
    login-url: http://localhost:3000/login

backend:
  upload:
    path: upload
  base:
    url: http://localhost:8080
ai:
  base:
    url: https://api.gravifox.com:8000

analyze:
  cors:
    allowed-origins: http://localhost:3000
  models:
    catalog-path: ../tvb-ai/api/models/catalog.json
